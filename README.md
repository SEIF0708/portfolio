# Saif Ben Abdallah — Portfolio
Next.js 16 · React 19 · Tailwind 4 · Framer Motion

    npm install && npm run dev      # http://localhost:3000/en
    npm run build

Content lives in `src/components/data.ts`. Layout and animations are in `src/components/Site.tsx`.
